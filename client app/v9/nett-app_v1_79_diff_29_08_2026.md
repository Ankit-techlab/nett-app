# nett-app v1.78 → v1.79 · change map · 29 Aug 2026
Base md5 `7762000f…` · Build md5 in `.md5` · 14 assert-anchored edits (`patch79.py`).

| # | Area | Change |
|---|---|---|
| 1 | goal page + verdict | ON RECORD status line removed (both `data-read` cards) |
| 2 | `#planm` | the orphaned `jplango` button stripped from the rendered recommendation |
| 3 | tag sheet | banner → count + total + `see which` toggle (`#tgnames`/`#tgnl`); capacity copy de-jargoned |
| 4 | copy | "regulated rails" (oconfirm) · "all three rails" (entry) · "waits for the rule book" (report) — all rewritten in the user's nouns |
| 5 | kit | **Gate 24** `scripts/audit-writing.js` — rendered-text lexicon sweep, red-proven on this build's own violations, now green |
| 6 | gate54 | jobdet assertion flipped: the page must NOT carry the status line |
